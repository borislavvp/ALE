using System;
using System.Collections.Generic;
using System.Text;

namespace logic.FiniteAutomataService.Helpers
{
    public static class DictionaryHelper
    {
        public static void Add<T,K>(this Dictionary<T,K> dictionary,T key, K value)
        {
            //    if(dictionary.ContainsKey(letter))
            //                    {
            //        foreach (var s in currentClosures)
            //        {
            //            var asd = statesBuilder[letter];
            //            asd.Add(s);
            //        }
            //    }
            //                    else
            //    {
            //        statesBuilder.Add(letter, currentClosures);
            //    }
        }
    }
}
